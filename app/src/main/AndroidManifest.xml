<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.tripplanner">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".homepage.MainHomePage"
            android:label="@string/title_activity_main_home_page"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".reminder.DialogActivity" />

        <service
            android:name=".reminder.ForegroundService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".reminder.TestAlarmManagerWithNotification" />
        <activity android:name=".homescreen.homeview.HomeActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/SplashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- >Manar <! -->
        <receiver android:name=".reminder.AlarmReciever" />
    </application>

</manifest>